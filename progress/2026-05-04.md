# Progress Log - 2026-05-04

## Diffusion Policy Model Check

- Found the private Hugging Face model repository:
  - `DependableDavid/g1-bottle-diffusion-policy`
- Confirmed the uploaded artifacts:
  - `checkpoints/g1_bottle_dp_14act_epoch25.ckpt`
  - `configs/g1_bottle_image.yaml`
  - `code/train_diffusion_unet_image_workspace.py`
  - `scripts/train_baseline_g1_bottle_image_5070ti.sh`
- Confirmed the policy was trained for the G1 bottle in/out dataset:
  - Dataset: `DependableDavid/g1-bottle-in-out`
  - Observation history: 2 frames
  - Image input: egocentric `color_0`, resized to `[3, 84, 84]`
  - State input: 23D left arm + left hand subset
  - Policy output: 14D left arm + left hand action chunk

## Repository Integration

- Forked `real-stanford/diffusion_policy` under the WashU GitHub account.
- Added the fork as a top-level submodule:
  - `Diffusion-Policy/`
- Added top-level DP helper script:
  - `run_dp.sh`
- Downloaded the private HF model artifacts into:
  - `/home/humanoid-pc/dp_runtime/models/g1-bottle-dp`
- Verified the main checkpoint is present locally:
  - `/home/humanoid-pc/dp_runtime/models/g1-bottle-dp/checkpoints/g1_bottle_dp_14act_epoch25.ckpt`
  - Size: about `1366.3 MiB`

## Real-Robot Deployment Scaffold

- Added `utils/dp_policy_server.py`:
  - Loads the original Diffusion Policy image workspace checkpoint.
  - Maintains the 2-frame image/state observation history.
  - Serves HTTP `/act`.
  - Returns the raw 14D policy action and a 31D debug expansion.
- Added `utils/dp_g1_client.py`:
  - Reuses `utils/vla_client.py` for G1 DDS control and camera input.
  - Defaults to dry-run.
  - Uses Enter-gated execution for real robot movement.
  - Supports `s` skip, `c` continuous, and `q` quit.
  - Executes only the 14D left arm + left hand action path.
  - Keeps the right side at its current state for the first deployment.

## Clarification

- The 31D action vector is only a debug/data-layout expansion that mirrors the original LeRobot action layout.
- The real robot client now executes the 14D policy output directly:
  - The policy still outputs 14D.
  - The client expands it through the checkpoint's selected action indices before extracting robot commands.
  - Left arm comes from expanded action indices `[0:7]`.
  - Left hand comes from expanded action indices `[14:21]`.

## Environment Work

- Started setting up the original Diffusion Policy runtime environment.
- The upstream environment file is:
  - `Diffusion-Policy/conda_environment.yaml`
- Its environment name is:
  - `robodiff`
- Created the conda environment at:
  - `/home/humanoid-pc/miniconda3/envs/robodiff`
- The upstream environment creation finished with a pip-stage failure while building `mujoco`:
  - Error: `MUJOCO_PATH environment variable is not set`
  - This is from simulation-related dependencies and is not needed for the first real-robot policy inference path.
- Installed the runtime pieces needed for deployment:
  - editable `diffusion_policy`
  - `fastapi`
  - `uvicorn`
  - `requests`
- Downgraded `huggingface_hub` to `0.13.4` inside `robodiff` because the pinned upstream `diffusers==0.11.1` imports `cached_download`, which was removed from newer `huggingface_hub` releases.

## Validation Results

- `bash run_dp.sh validate` passed.
- `bash run_dp.sh server --device cuda:0 --load-only` passed:
  - Checkpoint loaded successfully.
  - `use_ema=True`
  - `obs_horizon=2`
  - device: `cuda:0`
- Started the local DP HTTP server and ran:
  - `bash run_dp.sh client --mock --once`
- Mock client/server inference passed without connecting to the robot:
  - The server returned a 14D action chunk.
  - The client decoded it as left arm + left hand.
  - The client stayed in dry-run mode and applied large-delta clipping.

## Code Fixes During Environment Bring-up

- Fixed `utils/dp_policy_server.py` checkpoint loading:
  - Removed the incompatible `map_location` argument from `create_from_checkpoint`.
- Fixed `utils/dp_g1_client.py` mock mode:
  - Unitree SDK import is now delayed until real robot mode.
  - `--mock --once` can run in the DP environment without DDS/Unitree SDK installed.

## Real-Robot Test Preparation

- Added TeleImager startup/check support to `run_dp.sh`.
- New command:
  - `bash run_dp.sh camera`
- The camera helper checks for an existing stream first:
  - `tcp://192.168.123.164:5555`
- If no stream is detected, it SSHes into the humanoid robot as `unitree`, uploads `utils/robot_camera_server.py`, installs `pyzmq` if needed, and starts the camera server in the background.
- `bash run_dp.sh client ...` now starts/checks TeleImager automatically unless:
  - `--mock` is used
  - `--no-camera` is passed
- Recommended real-robot test order:
  - Start robot standing and balanced with the Unitree remote.
  - Start/check TeleImager: `bash run_dp.sh camera`.
  - Start policy server: `bash run_dp.sh server --device cuda:0`.
  - First run client in dry-run mode without `--execute`.
  - Only after dry-run observations look correct, run with `--execute` for Enter-gated action chunks.
- Current real-robot readiness check:
  - DP server `/health` returned `200 {"status":"ok"}`.
  - TeleImager stream was not detected yet on `tcp://192.168.123.164:5555`; it needs to be started from an interactive terminal with `bash run_dp.sh camera`.

## DP Real-Robot Client Safety Updates

- Confirmed TeleImager startup from this PC requires the humanoid robot SSH password unless the camera stream is already running.
- Added local robot-view visualization to `utils/dp_g1_client.py`:
  - Window name: `G1 DP Robot View`
  - Enabled by default in real robot mode.
  - Can be disabled with `--no-view`.
- Added teleop-style forward preparation before real execution:
  - Enabled by default when `--execute` is used.
  - Requires an explicit Enter confirmation before moving.
  - Phase 1: move through outward clearance with the right arm relaxed.
  - Phase 2: move to left-arm forward-ready pose with the right arm relaxed.
  - Can be disabled with `--no-prepare-forward`.
- Re-ran validation:
  - `bash -n run_dp.sh`
  - `python -m py_compile utils/dp_g1_client.py utils/dp_policy_server.py`
  - `bash run_dp.sh validate`
  - `bash run_dp.sh client --mock --once`

## DP Popup Control UI

- Added `--ui` mode to `utils/dp_g1_client.py`.
- The UI runs as a local web page, default:
  - `http://127.0.0.1:8030`
- It shows the robot camera stream and status.
- It has explicit buttons:
  - `Run Next Step`: runs one policy inference/action step only when pressed.
  - `Reset Forward Pose`: resets to the teleop-style left forward/right relaxed pose.
- In dry-run mode, the UI does not send robot commands.
- In `--execute` mode, `Run Next Step` sends one Enter-gated action step and `Reset Forward Pose` moves the robot through outward clearance into forward-ready pose.
- Added helper arguments:
  - `--ui-host`
  - `--ui-port`
- Validated the UI in mock mode:
  - `bash run_dp.sh client --mock --ui --ui-port 8031`
  - Checked `/` and `/status` returned HTTP 200.

## DP Action Chunk And Safety Update

- Added `Stop + Relax Arms` to the DP web UI.
- The stop path sets an interruption flag and, in execute mode, moves the arms through outward clearance into relaxed pose.
- Stop/reset also resets the DP policy server observation history through `/reset`.
- Changed real execution to ramp commands instead of sending each predicted target as a sudden position jump.
- Added action chunk prefix execution:
  - Default `--action-exec-steps 4`
  - Default `--step-seconds 0.25`
  - Each UI click executes a short smooth prefix of the returned 8-step action chunk.
- This is closer to the Psi0 RTC deployment idea:
  - infer a chunk,
  - execute only a short prefix,
  - keep the next click/observation responsible for replanning,
  - avoid discarding the whole chunk after only the first target.
- Checked the DP checkpoint inference configuration:
  - `horizon=16`
  - `n_obs_steps=2`
  - `n_action_steps=8`
  - `action_dim=14`
  - normalizer keys include `action`, `states`, and `color_0`
- Conclusion: manual state/action normalization should not be added outside the model because `DiffusionUnetImagePolicy.predict_action()` normalizes observations and unnormalizes actions internally from the checkpoint normalizer.
- Validated the updated mock UI:
  - `/step` handled a returned 8-step chunk and processed the configured 3-step prefix in mock mode.
  - `/stop` returned cleanly in dry-run mode.

## DP Finger And Waist Fix

- Found that the checkpoint's 14D action order is not simply `[left arm 7 + left hand 7]`.
- The training config uses selected action indices:
  - `[0, 1, 2, 3, 4, 5, 6, 15, 16, 17, 18, 19, 20, 30]`
- Fixed the client to expand 14D policy output into the 31D dataset layout before extracting:
  - left arm: `[0:7]`
  - left hand: `[14:21]`
- This avoids treating the final body/waist action dimension as a finger command.
- Changed hand behavior:
  - `--send-hands` is now enabled by default.
  - Use `--no-send-hands` to disable Dex3 hand commands.
  - `Reset Forward Pose` opens both hands after outward clearance.
  - `Stop + Relax Arms` also opens both hands after outward clearance.
- Changed DP client arm/waist gains:
  - `--arm-kp` default: `200`
  - `--arm-kd` default: `5`
  - These are passed into `G1Robot.send_arm_cmd()`, which controls waist and arms together through `rt/arm_sdk`.
- Waist handling remains conservative:
  - The client holds the current waist position.
  - The model's expanded body/waist action is printed/ignored rather than executed.

## DP UI Ctrl-C Safety Fix

- Found that Ctrl-C stopped the UI process but did not run the UI `Stop + Relax Arms` path, so the robot could remain in its current commanded pose.
- Added SIGINT/SIGTERM handling to `utils/dp_g1_client.py` UI mode.
- In execute mode, Ctrl-C / process shutdown now attempts to:
  - set the stop flag,
  - wait for the active action step to stop,
  - move through outward clearance,
  - open both Dex3 hands,
  - move arms into relaxed pose,
  - reset the DP policy observation history.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py utils/dp_policy_server.py`
  - `bash -n run_dp.sh`
  - `bash run_dp.sh validate`
  - mock UI SIGTERM exits cleanly.

## Unitree SDK Fix For Real-Robot DP Client

- Real robot UI launch initially failed after TeleImager check:
  - `Real robot mode requires vla_client and Unitree SDK imports. Import failed: No module named 'unitree_sdk2py'`
- Installed the existing local Unitree SDK source into the `robodiff` environment:
  - Source: `/home/humanoid-pc/unitree_sdk2_python`
  - Command: `/home/humanoid-pc/miniconda3/envs/robodiff/bin/python -m pip install -e /home/humanoid-pc/unitree_sdk2_python`
- This also installed `cyclonedds==0.10.2`.
- Verified imports in `robodiff`:
  - `unitree_sdk2py`
  - `vla_client`
  - `dp_g1_client`
- Re-ran `bash run_dp.sh validate` successfully.

## DP Waist Upright Fix

- Investigated why the waist did not look vertically balanced during DP UI execution.
- Found that reset, step execution, stop, and Ctrl-C relax were holding the current waist pose with `state["waist"].copy()`.
- This means any pre-existing waist roll/pitch offset would be preserved instead of corrected.
- Changed the DP client default to command an upright waist target:
  - `--waist-mode upright` sends `[0, 0, 0]` for waist during reset/action/stop/relax.
  - `--waist-mode current` remains available as a conservative fallback if the robot setup needs to hold its current waist pose.
- The same `--arm-kp 200 --arm-kd 5` gains still apply to waist and arms through `G1Robot.send_arm_cmd()`.
- Updated `run_dp.sh` help text with `--waist-mode`.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - `bash -n run_dp.sh`
  - Cursor lints for edited files.

## DP UI Start/Maintenance Split

- Clarified that the DP checkpoint still expects observations in the training state layout, so the client sends the 63D LeRobot-compatible state and the server selects the active 23D subset used by the model.
- The robot execution remains left-side only:
  - policy action is expanded from 14D to the 31D dataset layout,
  - only left arm and left hand are decoded for execution,
  - right arm is held/relaxed and waist is controlled by the local safety target.
- Split the web UI buttons into two blocks:
  - Start: `Preparation`, `Run Next Step`
  - Maintenance: `Reset`, `Stop + Relax Arms`
- Added `/prepare` as a separate UI endpoint that runs the same forward-pose preparation and policy-history reset as maintenance reset.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - `bash -n run_dp.sh`
  - `bash run_dp.sh validate`
  - Cursor lints for edited file.

## DP UI Stream Freshness Timestamp

- Added a timestamp overlay directly on the DP UI video stream:
  - `stream HH:MM:SS`
- Added `browser_status_refresh` to the status panel so the browser-side status polling freshness is visible too.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - Cursor lints for edited file.

## DP Joint Mapping Audit

- Re-checked whether all joints are handled correctly after observing weak inference quality.
- Confirmed Unitree arm command indexing in `utils/vla_client.py`:
  - waist: motor indices `12:15`
  - left arm: motor indices `15:22`
  - right arm: motor indices `22:29`
  - `rt/arm_sdk` command order is `[waist, left_arm, right_arm]`.
- Confirmed DP execution still only commands:
  - local waist target,
  - left arm,
  - left hand,
  - right arm held/relaxed.
- Found an important model-quality concern:
  - the checkpoint config's action indices are `[0..6, 15..20, 30]`,
  - this is not a clean `[left_arm 7 + left_hand 7]` selection,
  - it skips `left_hand[0]` and includes one body-action placeholder that is ignored during execution.
- This matches the current checkpoint layout, so changing it in the client without retraining would misalign the model output semantics.
- Added a UI `Joint Audit` button under Maintenance:
  - shows current waist/arm/hand values,
  - shows each model state dimension and source in the 63D runtime state,
  - shows each model action dimension and target in the 31D action layout.
- Recommendation for the next checkpoint:
  - retrain with clean left-only indices: state/action `[0..6, 14..20]`,
  - remove the ignored body-action placeholder.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - `bash run_dp.sh validate`
  - Cursor lints for edited file.

## DP UI Reset + Restart Model

- Renamed the Maintenance reset button to `Reset + Restart Model`.
- The reset path moves back to the forward pose and calls the DP server `/reset` endpoint.
- Confirmed `/reset` clears the server-side observation histories:
  - `image_history`
  - `state_history`
- This makes the next inference start fresh rather than depending on previous observations.
- Updated the server reset response message to make this behavior explicit.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py utils/dp_policy_server.py`
  - `bash run_dp.sh validate`
  - Cursor lints for edited files.

## DP Finger Safety Update

- Observed that policy finger outputs can suddenly close the Dex3 hand like grasping.
- Changed the DP client default hand behavior:
  - `--hand-mode open` is now the default during policy steps.
  - This keeps the left hand target at `OPEN_HAND_Q` instead of executing policy finger output.
  - `--hand-mode current` holds the current left hand state.
  - `--hand-mode policy` is still available, but should only be used after verifying finger mapping and model quality.
- Added hand interpolation inside each executed step so even policy hand targets are ramped over `--step-seconds` instead of being sent as an instantaneous jump.
- UI status now reports the active `hand_mode`, plus each step records both:
  - `policy_left_hand`
  - `executed_left_hand`
- Updated `run_dp.sh` help text with `--hand-mode`.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - `bash -n run_dp.sh`
  - `bash run_dp.sh validate`
  - Cursor lints for edited files.

## DP Text Conditioning Check

- Confirmed this Diffusion Policy checkpoint is not text-conditioned.
- The model observation is only:
  - `color_0`: RGB image history
  - `states`: low-dimensional proprioception history
- The UI/client task string is not passed into the DP model observation.
- The model knows the task because the checkpoint was trained for the fixed `g1-bottle-in-out` behavior, not because it reads a prompt.
- Updated the UI to show:
  - checkpoint task: `g1-bottle-in-out`
  - text prompt is not used by this DP model.
- Updated the DP server action response to include:
  - `text_conditioned: false`
  - `task: g1-bottle-in-out`
- Verified:
  - `python -m py_compile utils/dp_g1_client.py utils/dp_policy_server.py`
  - `bash run_dp.sh validate`
  - Cursor lints for edited files.

## DP UI Run 20 Cautious Button

- Added a high-visibility Start block button:
  - `ATTENTION/CAUTIOUS: Run 20 Model Inferences`
- The browser asks for confirmation before calling the new `/run-20` endpoint.
- `/run-20` runs up to 20 sequential calls to the existing one-step policy execution path.
- It keeps the same safety behavior as `Run Next Step`:
  - shared control lock,
  - current `--hand-mode`,
  - waist target mode,
  - arm delta clamps,
  - smooth step execution,
  - `Stop + Relax Arms` interruption through `stop_event`.
- Verified:
  - `python -m py_compile utils/dp_g1_client.py`
  - `bash run_dp.sh validate`
  - Cursor lints for edited file.

## Psi0 RTC UI Research And First Integration

- Researched the local Psi0 RTC path.
- Existing entry points:
  - server: `bash run_psi0.sh server-rtc RUN_DIR CKPT_STEP [PORT]`
  - client: `bash run_psi0.sh rtc-bimanual [options]`
- The RTC server is WebSocket-based at `/ws`, default port `8014`.
- The local G1 RTC client is `utils/psi0_rtc_bimanual_client.py`.
- It already reuses shared G1/camera utilities:
  - `CameraReceiver`
  - `G1Robot`
  - `G1Adapter`
  - `ensure_ai_mode`
- Added a first local web UI to `psi0_rtc_bimanual_client.py`:
  - launch with `--ui`
  - default address: `http://127.0.0.1:8040`
  - MJPEG robot camera view with stream timestamp
  - status panel with latest RTC action version and preview
  - `Run Next RTC Action`
  - `ATTENTION/CAUTIOUS: Run 20 RTC Actions`
  - `Stop + Return Arms`
  - `Resume Waiting`
- The UI approves actions from the existing `LatestActionBuffer`; it does not change the Psi0 WebSocket protocol.
- Safety behavior is inherited from the existing RTC client:
  - dry-run unless `--execute`
  - `ensure_ai_mode`
  - competing XR teleop process check
  - `G1Adapter._clamp_joints`
  - waist mode handling
  - hands are off unless `--send-hands`
  - stop returns arms using the configured `--exit-pose`
- Updated `run_psi0.sh` help text with `--ui` and `--ui-port`.
- Verified:
  - `python -m py_compile utils/psi0_rtc_bimanual_client.py`
  - `bash -n run_psi0.sh`

## Psi0 Continuous RTC Streaming

- User clarified that deployment should use RTC directly instead of approximating
  a chunk with client-side approval steps.
- Added UI controls:
  - `Start RTC Streaming`
  - `Pause RTC Streaming`
- Behavior:
  - default remains not streaming,
  - `Start RTC Streaming` sets `continuous=true`,
  - every incoming server RTC tick is executed until pause/relax,
  - `Pause RTC Streaming` sets `continuous=false`, pauses execution, and clears
    approval budget,
  - `Relax Arms` still pauses, clears budget, relaxes arms, and opens hands.
- Kept `Run Next RTC Chunk` only as a cautious/debug option.
- Restarted the Psi0 UI client with:
  - `--send-hands`
  - `--arm-side left`
  - `--approval-steps 15`
- Confirmed UI:
  - contains `Start RTC Streaming`,
  - contains `Pause RTC Streaming`,
  - starts with `continuous=false`,
  - starts with `approve_budget=0`.
- Updated `docs/dp_psi0_deploy_commands.md` to recommend:
  - Preparation,
  - then Start RTC Streaming for normal RTC deployment,
  - Pause RTC Streaming or Relax Arms to stop execution.

## Psi0 Left-Only Right Arm Hold Fix

- During real-robot testing, left-only mode still moved the right arm:
  - `Preparation` moved both arms to the preparation pose,
  - policy execution moved the right arm to the relaxed pose on the first
    approved/streamed action.
- Paused RTC streaming with `POST /pause-rtc` before editing.
- Changed left-only behavior:
  - policy decode now keeps `right_arm = previous_target[7:14]`,
  - right arm is no longer forced to relaxed during left-only policy execution,
  - right hand still stays open in left-only mode.
- Changed Preparation behavior:
  - in `--arm-side left`, Preparation only moves the left arm,
  - right arm target is copied from the current robot right arm,
  - hands are still opened by Preparation.
- Updated `docs/dp_psi0_deploy_commands.md` to clarify that `--arm-side left`
  holds the right arm at its current target instead of relaxing it.
- Verified:
  - `python -m py_compile utils/psi0_rtc_bimanual_client.py`
  - Cursor lints for `utils/psi0_rtc_bimanual_client.py`
  - Cursor lints for edited files.

## Psi0 RTC UI Task Preset Selector

- Added task preset selection to the Psi0 RTC UI.
- The UI now loads task presets from `tasks/task_list.json`.
- The Start block shows:
  - task preset dropdown,
  - selected task name,
  - selected language prompt.
- Changing the dropdown calls `/task`, updates UI status, and updates the WebSocket client's instruction string.
- Future observation messages sent to the Psi0 RTC server use the selected prompt in the `instruction` field.
- The prompt is built from each task's `goal`, `desc`, and `steps` fields.
- Verified:
  - `python -m py_compile utils/psi0_rtc_bimanual_client.py`
  - `bash -n run_psi0.sh`
  - Cursor lints for edited file.

## Psi0 HF Checkpoint Download

- Checked Hugging Face account `DependableDavid`.
- Found Psi0 checkpoint repo:
  - `DependableDavid/psi0-g1-bottle`
- Repo contains checkpoint directories for:
  - `ckpt_5000`
  - `ckpt_10000`
- Each checkpoint directory on HF includes optimizer/training state files:
  - `optimizer.bin`
  - `scheduler.bin`
  - `random_states_*.pkl`
- Downloaded only model weights:
  - `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm/checkpoints/ckpt_5000/model.safetensors`
  - `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm/checkpoints/ckpt_10000/model.safetensors`
- Confirmed local checkpoint directories contain only:
  - `model.safetensors`
- Optimizer, scheduler, and random state files were not downloaded into the runtime checkpoint directories.

## Psi0 10k Memory Safety Guard

- Attempted to start the Psi0 10k RTC server:
  - run dir: `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm`
  - checkpoint: `ckpt_10000`
  - port: `8014`
- Filled the missing run metadata required by the upstream loader:
  - `run_config.json`
  - `argv.txt`
- Initialized Psi0's `third_party/SIMPLE` submodule and nested submodules so
  `uv run --group psi --group serve` can resolve local editable dependencies.
- Confirmed `ckpt_10000/model.safetensors` contains inference weights for:
  - `vlm_model.*`: 625 tensors
  - `action_header.*`: 181 tensors
- Confirmed optimizer, scheduler, and random state files are still not present
  locally.
- Stopped retrying unconstrained loading after Cursor/system memory instability.
- Added memory safeguards to `run_psi0.sh`:
  - `PSI0_MIN_AVAILABLE_RAM_GB`, default `18`
  - `PSI0_SERVER_MEMORY_GB`, default `24`
  - server start now performs a RAM preflight check before `uv run`
  - server process is capped with user `systemd-run --scope` when available
  - fallback uses `ulimit` if user systemd scopes are unavailable
- Reduced Psi0 loader CPU memory pressure:
  - construct the Qwen3VL backbone directly in bf16
  - release VLM/action checkpoint dictionaries after loading
- Verified:
  - `bash -n run_psi0.sh`
  - `python -m py_compile Psi0/src/psi/models/psi0.py`
  - RAM preflight blocks unsafe startup when the required RAM is unavailable.

## DP and Psi0 Deployment Documentation

- Added English deployment command documentation:
  - `docs/dp_psi0_deploy_commands.md`
- The doc records the recommended two-terminal layout:
  - Terminal 1: model server
  - Terminal 2: robot-facing UI/client
- Documented DP deployment:
  - Terminal 1 runs `bash run_dp.sh server --host 0.0.0.0 --port 8020 --device cuda:0`
  - Terminal 2 runs `bash run_dp.sh client ... --ui --execute`
  - UI opens at `http://127.0.0.1:8030`
- Documented Psi0 RTC 10k deployment:
  - Terminal 1 runs `bash run_psi0.sh server-rtc ... 10000 8014`
  - Terminal 1 command includes memory guards:
    - `PSI0_SERVER_MEMORY_GB=22`
    - `PSI0_MIN_AVAILABLE_RAM_GB=20`
  - Terminal 2 runs `bash run_psi0.sh rtc-bimanual ... --ui --execute`
  - UI opens at `http://127.0.0.1:8040`
- Listed the main flags operators should care about for both DP and Psi0.
- Included safe dry-run/preflight checks.

## Psi0 10k Shape Mismatch Fix

- Observed server load failure when using copied baseline config:
  - checkpoint `obs_proj._obs_proc.1.weight`: `[1536, 63]`
  - copied config model expected: `[1536, 36]`
  - checkpoint `action_proj_in.dec_pos`: `[30, 1536]`
  - copied config model expected: `[16, 1536]`
  - checkpoint action dim: `31`
  - copied config action dim: `36`
- Confirmed 10k checkpoint shape directly from `model.safetensors`:
  - state / obs dim: `63`
  - action dim: `31`
  - action chunk size: `30`
- Updated local 10k metadata:
  - `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm/run_config.json`
  - `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm/argv.txt`
- Corrected local config values:
  - `pad_state_dim=63`
  - `pad_action_dim=31`
  - `model.odim=63`
  - `model.action_dim=31`
  - `model.action_chunk_size=30`
  - `model.action_exec_horizon=22`
- Patched Psi0 RTC server history-action replacement to handle 31D actions:
  - 31D action means `14 hand + 14 arm + 3 torso rpy`
  - no predicted height action is available
  - server now preserves `obs[31:]` instead of trying to splice an empty
    height action
- Verified:
  - run config shape assertions pass
  - `python -m py_compile Psi0/src/psi/deploy/psi_serve_rtc-trainingtimertc.py Psi0/src/psi/models/psi0.py`

## Psi0 Real-Robot Test Start

- Started the Psi0 10k RTC server with memory guards:
  - `PSI0_SERVER_MEMORY_GB=22`
  - `PSI0_MIN_AVAILABLE_RAM_GB=20`
  - run dir: `/home/humanoid-pc/psi0_runtime/runs/g1-bottle-left-arm`
  - checkpoint: `10000`
  - port: `8014`
- Confirmed server load succeeded:
  - VLM backbone checkpoint loaded
  - action head checkpoint loaded
  - `uvicorn` listening on `0.0.0.0:8014`
  - no shape mismatch after correcting `31D/63D/30` metadata
- First Psi0 UI client launch failed because robot had rebooted and was not
  yet reporting `ai` balance mode.
- After the robot was rebooted and stood up, relaunched the Psi0 UI client:
  - UI listening on `http://127.0.0.1:8040`
  - robot mode check passed: `ai`
  - WebSocket connected to the Psi0 RTC server
- Found robot camera was not streaming after reboot:
  - `tcp://192.168.123.164:5555` did not return frames
  - operator should run `bash run_dp.sh camera` in an interactive terminal to
    restart the robot-side camera server.

## Psi0 UI Preparation Button

- Added a Start-block button:
  - `Preparation: Move To Ready Pose`
- Added `POST /prepare` to the Psi0 UI.
- Preparation behavior:
  - pauses RTC execution while moving,
  - clears pending approval budget,
  - ramps both arms to a conservative ready pose,
  - keeps the configured waist mode,
  - opens both Dex3 hands even when normal policy hand execution is disabled,
  - resumes waiting for manual RTC action approval after the move.
- Synced the UI control loop's hold target after preparation so the robot does
  not move back to the previous target after the preparation ramp.
- Restarted the Psi0 UI client and confirmed:
  - `http://127.0.0.1:8040` is live,
  - page HTML contains `Preparation: Move To Ready Pose`,
  - WebSocket connection to the Psi0 server is active.
- Updated `docs/dp_psi0_deploy_commands.md` to mention the Psi0 Preparation
  button and the recommended order:
  - restart camera,
  - verify video,
  - press Preparation,
  - run one RTC action at a time.
- After real-robot testing, fixed Preparation so it explicitly sends open-hand
  commands for both Dex3 hands. The regular Psi0 policy path still keeps hands
  disabled unless `--send-hands` is passed.

## Psi0 UI Relax Arms

- During real-robot testing, `Stop + Return Arms` was unclear and did not use
  the same relaxed arm posture as the DP UI.
- Changed the Psi0 UI maintenance button text:
  - from `Stop + Return Arms`
  - to `Relax Arms`
- Changed the default exit/relax target from all-zero arms to the same style
  relaxed bent-arm pose used by the DP client.
- `Relax Arms` now:
  - pauses the control loop,
  - clears pending RTC approval budget,
  - moves arms to the relaxed pose,
  - opens both Dex3 hands,
  - leaves the UI paused.
- `Resume Waiting` now clears approval budget and only resumes waiting for a
  future manual approval. It does not execute any buffered action by itself.
- Killed stale duplicate Psi0 UI client processes and restarted one fresh UI
  client on `http://127.0.0.1:8040`.
- Confirmed:
  - only one Psi0 UI client process remains,
  - page contains `Relax Arms`,
  - old `Stop + Return Arms` text is gone,
  - server remains listening on `0.0.0.0:8014`.

## Remote L2+UP Safety Note

- Observed a real-robot safety issue: pressing `L2 + UP` while a Python robot
  client is still publishing to `rt/arm_sdk` can make the robot move toward the
  client's last commanded forward/ready target instead of the expected slow
  home/stand-ready behavior.
- Immediately stopped all robot-commanding clients:
  - `utils/psi0_rtc_bimanual_client.py`
  - `utils/dp_g1_client.py`
  - `utils/vla_client.py`
- Confirmed no robot-commanding clients remained.
- Left the Psi0 model server running on `8014` because it does not directly
  command the robot.
- Updated `run_vla.sh` remote-controller instructions with a bilingual warning:
  - stop all `rt/arm_sdk` robot-control clients before pressing `L2 + UP`,
  - otherwise the active client can fight the remote command.
- Verified:
  - `bash -n run_vla.sh`

## Psi0 Hand Policy Enabled

- User requested enabling hand policy for Psi0 real-robot testing.
- Restarted the Psi0 UI client with:
  - `--send-hands`
- Confirmed UI status:
  - `send_hands: true`
  - `approve_budget: 0`
- Behavior after this change:
  - approved RTC actions send both policy arm targets and policy hand targets,
  - `Preparation` still forces both hands open,
  - `Relax Arms` still opens both hands,
  - no action executes until the operator presses `Run Next RTC Action`.
- Updated `docs/dp_psi0_deploy_commands.md` to include `--send-hands` for
  the active Psi0 testing command.

## Psi0 Left-Only RTC Execution

- User observed two deployment issues:
  - the policy was moving the right arm, but the current bottle test should use
    only the left arm,
  - one UI approval did not produce much movement.
- Found the cause:
  - the client decoded Psi0 actions as bimanual by default,
  - `Run Next RTC Action` approved only one 30Hz RTC sample, which is too short
    to look like a meaningful chunk.
- Added `--arm-side` to `utils/psi0_rtc_bimanual_client.py`:
  - default: `left`,
  - `left` executes only policy left arm and left hand,
  - right arm is held at the relaxed pose,
  - right hand is held open,
  - `bimanual` is still available for future two-arm testing.
- Added `--approval-steps`:
  - default: `15`,
  - one UI `Run Next RTC Chunk` click now approves 15 RTC ticks,
  - this is about 0.5s at the server's 30Hz RTC loop.
- Updated UI wording:
  - `Run Next RTC Action` -> `Run Next RTC Chunk`
  - status reports `arm_side` and `approval_steps`.
- Restarted the Psi0 UI client with:
  - `--send-hands`
  - `--arm-side left`
  - `--approval-steps 15`
- Confirmed UI status:
  - `arm_side: left`
  - `approval_steps: 15`
  - `send_hands: true`
  - `approve_budget: 0`
- Updated `run_psi0.sh` help text and
  `docs/dp_psi0_deploy_commands.md`.
- Verified:
  - `python -m py_compile utils/psi0_rtc_bimanual_client.py`
  - `bash -n run_psi0.sh`
